<!-- Sec --> 
<!-- <button class="new-employee-position mg-btn-bottom-margin submit mc-margin-bottom-20 flat-button" id="newEmployeePositionID">New Profile</button> -->
<a class="createNewTestSetting" href= "/mg_employee_positions/new" id="createNewTestSetting">
<button id='createNew_Test_SettingsID' class="mg-btn-bottom-margin submit mc-margin-bottom-20 flat-button">
New Profile </button></a>
<h4 class="form-section-heading mc-margin-bottom-20">Listing Profile</h4>
<% if @employee_positions.length > 0%>
<div class="mg_pagination">
    <table class="table table-bordered table-striped mg-tbl-margin mg-tbl-margin" id="EmpCatTableID">
    <thead>
        <tr>
          <th><%= t('employeecategory') %></th>
          <th><%= t('profilename') %></th>
          <!-- <th><%#= t('status') %></th> -->
          <th><%= t('actions') %></th>
        </tr>
       </thead>
        <tbody>
        <% @employee_positions.each do |employee_position| %>
           <%= render partial: 'table_row', locals: {employee_position: employee_position} %>
        <% end %>
           </tbody>
    </table>
     <%#= will_paginate @employee_positions, :container => false %>
    </div>
<% else %>
  <h4>Please create some employee position... </h4>

<% end %>
<div id="newEmployeePositionDialogID" ></div>